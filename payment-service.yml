server:
  port: 8084

spring:
  application:
    name: payment-service
  datasource:
    url: jdbc:mysql://localhost:3306/payment_db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

spring.security:
  user:
    name: admin
    password: admin
